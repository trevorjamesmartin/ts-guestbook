{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "17.9.0",
    "npm": "8.5.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "start:dev": "npx nodemon -r dotenv/config -V --no-stdin -w src index.ts",
    "create:db": "npx knex migrate:latest && npx knex seed:run",
    "build:client": "cd src/app;npm i && npm run build",
    "build": "npm run build:client",
    "develop": "npm run build && npm run start:dev",
    "example": "NODE_ENV=demo npm run develop",
    "precompile": "npm run build",
    "compile": "tsc",
    "release:scripts": "npx ts-node scripts/release-package.ts && rm -rf dist/scripts",
    "prepackage": "mkdir -p dist/src/app/build && cp -rp src/app/build dist/src/app && mkdir -p release && npm run release:scripts",
    "package": "tar czvf dist.tar.gz dist/* && mkdir -p release && mv dist.tar.gz release",
    "clean": "rm -rf dist/* >> /dev/null 2>&1;rm dist.tar.gz >> /dev/null 2>&1;rm release/* >> /dev/null 2>&1",
    "release": "npm run compile && npm run package",
    "contain": "cd release;docker build . -t vigilant-cloud",
    "start:container": "docker run -v $(pwd)/db:/opt/app/db -p 8080:8080 -d vigilant-cloud",
    "stop:container": "docker ps|grep vigilant-cloud|awk '{print $1}' | xargs docker stop",
    "build:container": "npm run clean && npm run release && npm run contain",
    "dockerize": "npm run stop:container >> /dev/null 2>&1;npm run build:container && npm run start:container",
    "deploy": "./scripts/build-javascript.sh && ./scripts/deploy-to-heroku.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@socket.io/postgres-adapter": "^0.3.0",
    "@socket.io/redis-adapter": "^7.2.0",
    "@vscode/sqlite3": "^5.0.8",
    "aws-sdk": "^2.1132.0",
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "ejs": "^3.1.8",
    "express": "^4.17.3",
    "express-content-length-validator": "^1.0.0",
    "express-enforces-ssl": "^1.1.0",
    "express-validator": "^6.14.0",
    "helmet": "^5.0.2",
    "host-validation": "^2.0.1",
    "hpp": "^0.2.3",
    "jsonwebtoken": "^8.5.1",
    "knex": "^1.0.5",
    "log4js": "^6.5.1",
    "pg": "^8.7.3",
    "redis": "^4.1.0",
    "serve-favicon": "^2.5.0",
    "socket.io": "^4.5.0",
    "swagger-jsdoc": "^6.2.1",
    "swagger-ui-dist": "^4.11.1",
    "swagger-ui-express": "^4.4.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/cors": "^2.8.12",
    "@types/ejs": "^3.1.0",
    "@types/express": "^4.17.13",
    "@types/express-enforces-ssl": "^1.1.1",
    "@types/host-validation": "^2.0.1",
    "@types/hpp": "^0.2.2",
    "@types/jsonwebtoken": "^8.5.8",
    "@types/node": "^17.0.23",
    "@types/serve-favicon": "^2.5.3",
    "@types/swagger-jsdoc": "^6.0.1",
    "@types/swagger-ui-express": "^4.1.3",
    "@types/uuid": "^8.3.4",
    "dotenv": "^16.0.0",
    "nodemon": "^2.0.15",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.6",
    "utf-8-validate": "^5.0.9"
  }
}
